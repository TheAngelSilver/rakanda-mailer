export const theme = {
  colors: {
    coffee: {
      light: "#D4A574",
      medium: "#8B4513", // Adding this to match Button component
      dark: "#3B2515",
    },
  },
};

export type Theme = typeof theme;
